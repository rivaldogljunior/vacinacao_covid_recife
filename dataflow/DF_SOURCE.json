{
	"name": "DF_SOURCE",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "ds_csv_generic",
						"type": "DatasetReference"
					},
					"name": "sourceApi"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "dl_parquet_generic",
						"type": "DatasetReference"
					},
					"name": "sinkToTrusted"
				}
			],
			"transformations": [
				{
					"name": "aggregateCpf"
				},
				{
					"name": "filterCpfNull"
				},
				{
					"name": "aggregateDuplicateRow"
				}
			],
			"scriptLines": [
				"source(output(",
				"          cpf as string,",
				"          nome as string,",
				"          sexo as string,",
				"          grupo as string,",
				"          vacina as string,",
				"          lote as string,",
				"          dose as short,",
				"          data_vacinacao as date,",
				"          local_vacinacao as string",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> sourceApi",
				"sourceApi aggregate(groupBy(cpf,",
				"          nome),",
				"     dose = count(cpf),",
				"          sexo = last(sexo),",
				"          grupo = last(grupo),",
				"          first_data_vacinacao = last(data_vacinacao),",
				"          last_data_vacinacao = first(data_vacinacao),",
				"          first_local_vacinacao = last(local_vacinacao),",
				"          last_local_vacinacao = first(local_vacinacao)) ~> aggregateCpf",
				"aggregateCpf filter(cpf!='null') ~> filterCpfNull",
				"filterCpfNull aggregate(each(match(), ERROR_FUNCTION('') = ERROR_FUNCTION(''))) ~> aggregateDuplicateRow",
				"aggregateDuplicateRow sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     format: 'parquet',",
				"     partitionFileNames:['setVacinadosTrusted.parquet'],",
				"     umask: 0022,",
				"     preCommands: [],",
				"     postCommands: [],",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     saveOrder: 1,",
				"     partitionBy('hash', 1)) ~> sinkToTrusted"
			]
		}
	}
}