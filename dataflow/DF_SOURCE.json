{
	"name": "DF_SOURCE",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "ds_csv_generic",
						"type": "DatasetReference"
					},
					"name": "sourceApi"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "dl_parquet_generic",
						"type": "DatasetReference"
					},
					"name": "sinkToTrusted"
				}
			],
			"transformations": [
				{
					"name": "DistinctRows"
				},
				{
					"name": "aggregateCpfNome"
				},
				{
					"name": "filterCpfNull"
				}
			],
			"scriptLines": [
				"source(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     inferDriftedColumnTypes: true,",
				"     ignoreNoFilesFound: false) ~> sourceApi",
				"sourceApi aggregate(groupBy(mycols = sha2(256,columns())),",
				"     each(match(true()), $$ = first($$))) ~> DistinctRows",
				"DistinctRows aggregate(groupBy(cpf,",
				"          nome),",
				"     dose = count(cpf),",
				"          sexo = last(sexo),",
				"          grupo = last(grupo)) ~> aggregateCpfNome",
				"aggregateCpfNome filter(cpf!='null' && dose <= 3) ~> filterCpfNull",
				"filterCpfNull sink(allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     format: 'parquet',",
				"     partitionFileNames:['setVacinadosTrusted.parquet'],",
				"     umask: 0022,",
				"     preCommands: [],",
				"     postCommands: [],",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     saveOrder: 1,",
				"     partitionBy('hash', 1)) ~> sinkToTrusted"
			]
		}
	}
}